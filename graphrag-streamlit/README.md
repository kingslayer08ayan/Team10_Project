# GraphRAG Literature Explorer (lightweight edition)

A GraphRAG-style retrieval pipeline over your own PDFs: PDFs → chunks → entity/relation graph → HyDE-enhanced retrieval,
all through a Streamlit UI.
